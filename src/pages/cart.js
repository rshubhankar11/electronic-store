import React from "react";
import Base from "../components/Base";

const Cart = () => {
  return (
    <Base
      title="Cart"
      buttonFlag="True"
      buttonType="outline-warning"
      description={null}
    >
      <div>
        <h1>Cart Page</h1>
      </div>
    </Base>
  );
};

export default Cart;
